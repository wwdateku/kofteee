apiVersion: v1
kind: Service
metadata:
  name: seesdvk
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "seesdvk"
    port: 8080
  selector:
    app: seesdvk
